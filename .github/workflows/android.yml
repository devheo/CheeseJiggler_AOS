name: Android CI/CD

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v*'
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v4
    
    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Decode Keystore
      if: startsWith(github.ref, 'refs/tags/v')
      run: |
        echo "${{ secrets.SET_SIGNING_NICSY_STORE_FILE_BASE64 }}" | base64 --decode > release.jks

    - name: Build with Gradle
      env:
        SET_SIGNING_NICSY_STORE_FILE_PATH: ../release.jks
        SET_SIGNING_NICSY_STORE_PASSWORD: ${{ secrets.SET_SIGNING_NICSY_STORE_PASSWORD }}
        SET_SIGNING_NICSY_KEY_ALIAS: ${{ secrets.SET_SIGNING_NICSY_KEY_ALIAS }}
        SET_SIGNING_NICSY_KEY_PASSWORD: ${{ secrets.SET_SIGNING_NICSY_KEY_PASSWORD }}
      run: |
        if [[ $GITHUB_REF == refs/tags/v* ]]; then
          VERSION_NAME=${GITHUB_REF#refs/tags/v}
          ./gradlew assembleProdRelease -PversionCode=${{ github.run_number }} -PversionName=$VERSION_NAME
        else
          ./gradlew assembleDevDebug -PversionCode=${{ github.run_number }}
        fi

    - name: Rename and Prepare APK
      if: startsWith(github.ref, 'refs/tags/v')
      run: |
        TAG_NAME=${GITHUB_REF#refs/tags/}
        mv app/build/outputs/apk/prod/release/app-prod-release.apk app/build/outputs/apk/prod/release/CheeseJiggler_$TAG_NAME.apk

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-apk
        path: app/build/outputs/apk/**/*.apk

    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v2
      with:
        files: app/build/outputs/apk/prod/release/CheeseJiggler_*.apk
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
